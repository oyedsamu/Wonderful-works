// function greet() {
//     console.log("hello pal")
// };

// greet()

// const speak = function (){          //This is an expression having = sign. It must end with a ;

// }

// /* A function declaration is hoisted directly to the top of the code while
// a variable expression of a function is not hoisted. This means you can effectively
// declare your function at the very end but still call it prior to its place in the 
// code*/

// /*The argument/ parameter in the () immediately after a function decalrartion is like a locally
// decalred variable which is expected to work within the function alone. It is 
// expected to hold a value when you're using it in a calling of a function, 
// else it returns undefined */

// /* When you need more than one value to be used in a function, you declare them 
// along by separating with a comma same way you define multiple variables. The 
// order must be in the same way as the parameter */

// /* We can assign a default value by declaring the function with a name value pair method.
// This means that whenever you call the function with no value (), it will automatically
// fall back to use the default values.*/

// /* Return gives a value that is stored and can be retrieved later as a const. Return unlike 
// console.log does not need parenthesis. You return directly */

// const greet = function(){
//     return 'hello world';
// }




// const greet = hello => 'hello world';
// const hello = greet(hello);


